Standard home directory setup.

Execute with

```
curl -s https://raw.githubusercontent.com/sjennison/default-home-dir/master/install.sh | bash
```

Sets up bash profile, bash settings, and SSH settings
